let numX;
let numY;
let numZ;
let promedio = (numX + numY + numZ) /3;
console.log(promedio);

numX = 5;
numY = 7;
numZ = 9;
promedio = (numX + numY + numZ) /3;
console.log(promedio);

numX = -2;
numY = -3;
numZ = -8;
promedio = (numX + numY + numZ) /3;
console.log(promedio);

numX = -1.2;
numY = 5.3;
numZ = -3.3;
promedio = (numX + numY + numZ) /3;
console.log(promedio);